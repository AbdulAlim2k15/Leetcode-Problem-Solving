class Solution(object):
    def countNegatives(self, grid):
        m = len(grid)
        n = len(grid[0])
        negCounter = 0
        for i in range(m):
            for j in range(n):
                if grid[i][j] < 0:
                    negCounter += 1
        return negCounter

        
